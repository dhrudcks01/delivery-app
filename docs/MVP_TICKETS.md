# MVP 티켓 백로그 (쓰레기 수거 서비스)

## 사용 규칙 (중요)
- Codex는 작업마다 "티켓 1개"만 수행한다.
- 티켓 범위 밖 변경(리팩토링/추가 기능)은 금지한다. 필요하면 제안만 한다.
- 완료 시 반드시 아래를 출력한다:
  1) 변경된 파일 목록
  2) 실행/검증 방법(명령어)
  3) 주요 설계 및 주의사항 요약
  4) 테스트 추가/실행 결과

---

## 상태 표기
- [ ] TODO
- [~] IN PROGRESS
- [x] DONE

---

# 서비스 MVP 핵심 흐름 (요약)
1) USER: 쓰레기 수거 신청 생성(주소/연락처/요청사항)
2) OPS_ADMIN: 기사 배정(ASSIGNED)
3) DRIVER: 현장 무게 입력 + 사진 업로드(MEASURED)
4) 서버: 요금 계산(무게 기반) → 저장된 카드로 자동 결제 시도
5) 결제 성공: PAID → COMPLETED
6) 결제 실패: PAYMENT_FAILED(운영자 확인/재시도/결제수단 재등록)

---

# EPIC 0) 프로젝트/개발환경

### [x] T-0001 레포 구조 및 README 작성

### [x] T-0002 서버(Spring Boot) 스캐폴딩 생성
**Goal**
- Spring Boot 3 + Java + Gradle 프로젝트 생성

**DoD**
- `/health` 200 응답

---

### [~] T-0003 DB 연결 가이드 문서 (로컬/원격, Docker 없이)
**배경**
- 공개 레포이므로 시크릿/비밀번호 하드코딩 금지

**Goal**
- Windows/Mac에서 Docker 없이 로컬 또는 원격(MySQL) 연결 가능하도록 문서화
- 환경변수(DB_URL/DB_USERNAME/DB_PASSWORD) 방식만 사용

**DoD**
- `docs/dev/mysql-connection.md` 작성
- `.env` 사용 방법(예시 값은 placeholder) + `.gitignore` 확인
- 기존 `docs/dev/mysql-local.md`가 있다면, 내용 정리/통합(선택)

---

### [x] T-0004 Flyway 적용 (초기 마이그레이션)

### [x] T-0005 표준 에러 응답 및 예외 처리

### [x] T-0006 OpenAPI(Swagger) 노출

---

# EPIC 1) 인증 / 계정 / 권한 (RBAC)

### [x] T-0101 DB: users / auth_identities / roles / user_roles

### [x] T-0102 SYS_ADMIN 초기 계정 생성(부트스트랩)
**Goal**
- 로컬에서 SYS_ADMIN 계정으로 로그인 가능

**DoD**
- 운영환경/공개레포 고려: 초기 비밀번호/시크릿은 환경변수 또는 로컬 전용 설정에서만
- 문서로 생성/접속 방법 안내

---

### [x] T-0103 이메일 회원가입 및 로그인 + JWT 발급

### [x] T-0104 Refresh 토큰 재발급
### [x] T-0105 /me (roles 포함)
### [x] T-0106 RBAC 서버 강제 적용
**Goal**
- USER / DRIVER / OPS_ADMIN / SYS_ADMIN 권한 분리

**DoD**
- 권한 없는 요청은 403
- 최소 1개 이상 테스트 포함

### [x] T-0107 백엔드 API 공통 요청/응답 로그(운영용)
**Goal**
- 전체 API 호출에 대해 추적 가능한 공통 로그를 남긴다.

**DoD**
- 요청 단위 `requestId` 생성/전파(MDC)
- 요청 메서드/URI/상태코드/처리시간(ms)/클라이언트 IP 로그 출력
- 인증된 요청은 userId/email(가능한 범위) 포함
- 민감정보(비밀번호/토큰/카드관련 값) 마스킹 또는 로그 제외

---

### [x] T-0108 사용자 접속 기록(로그인 성공/실패) 저장
**Goal**
- 사용자 접속 이력을 DB에 저장해 운영 추적이 가능하도록 한다.

**DoD**
- DB: 접속기록 테이블(Flyway) 추가
- 로그인 성공/실패 시각, 식별자(email), IP, User-Agent, 결과를 저장
- 조회 API는 최소 OPS_ADMIN/SYS_ADMIN용으로 제공(페이징)
- 개인정보 최소수집 원칙 준수(필요 필드만 저장)

---

### [ ] T-0109 카카오 OAuth 로그인(서버)
**Goal**
- 카카오 계정으로 로그인/회원가입이 가능하도록 서버 플로우를 제공한다.

**DoD**
- OAuth 시작/콜백 endpoint 구현
- 카카오 사용자 식별값을 `auth_identities(provider=KAKAO)`에 저장
- 기존 이메일 계정과 충돌 정책 정의 및 처리
- JWT(Access/Refresh) 발급까지 기존 로그인 흐름과 동일하게 동작

---

### [ ] T-0110 구글 OAuth 로그인(서버)
**Goal**
- 구글 계정으로 로그인/회원가입이 가능하도록 서버 플로우를 제공한다.

**DoD**
- OAuth 시작/콜백 endpoint 구현
- 구글 사용자 식별값을 `auth_identities(provider=GOOGLE)`에 저장
- 기존 이메일 계정과 충돌 정책 정의 및 처리
- JWT(Access/Refresh) 발급까지 기존 로그인 흐름과 동일하게 동작

---

### [x] T-0111 로그인 식별자 검증 완화(email 전용 -> string)
**Goal**
- 로그인 식별자를 이메일 형식에 고정하지 않고 일반 문자열로 받아 인증 가능하게 한다.

**DoD**
- `/auth/login` 요청 DTO에서 email 형식 강제 검증 제거
- 인증 조회 키 정책 정리(예: email 컬럼을 문자열 식별자로 사용)
- 기존 계정 로그인 호환성 유지
- 실패 응답/에러 메시지 규칙 기존 포맷 유지

---

### [x] T-0112 도로명 주소 검색 API 연동(서버)
**Goal**
- 모바일에서 주소 검색 시 사용할 수 있도록 서버에 도로명 주소 검색 API 연동 endpoint를 제공한다.

**DoD**
- 외부 주소 API 연동용 서비스/설정값(키/URL) 추가
- 검색어 기반 주소 검색 endpoint 제공(페이징 또는 개수 제한 포함)
- 장애/타임아웃 시 표준 에러 응답으로 처리
- API 키/시크릿은 환경변수로만 관리

---

### [x] T-0113 회원가입 식별자 검증 완화(email 전용 -> string)
**Goal**
- 회원가입 식별자를 이메일 형식에 고정하지 않고 일반 문자열(ID)로 가입 가능하게 한다.

**DoD**
- `/auth/register` 요청 DTO에서 email 형식 강제 검증 제거
- 식별자 중복 검증 정책 유지(기존 사용자와 충돌 방지)
- 기존 이메일 기반 계정과 로그인 호환성 유지
- 모바일 회원가입 화면 검증 규칙도 문자열 식별자 정책과 일치

---

### [x] T-0114 로그인 실패 사유 분리(ID 없음 / 비밀번호 불일치)
**Goal**
- 로그인 실패 원인을 운영/사용자 안내 관점에서 구분 가능하게 한다.

**DoD**
- ID 미존재와 비밀번호 불일치 케이스를 서버에서 분리 처리
- 표준 에러 포맷을 유지하면서 코드/메시지 구분
- 모바일 로그인 화면에서 분리된 에러 메시지 노출
- 로그인 실패 이력 저장 정책(T-0108)과 충돌 없도록 정리

---

### [x] T-0115 도로명 주소 검색 API Juso 스펙 정합화(서버)
**Goal**
- 현재 범용 주소 검색 연동(T-0112)을 Juso 검색 API 스펙에 맞게 정식 연동하여, 운영 키(confmKey)로 안정적으로 검색되도록 한다.

**DoD**
- `app.address-search` 설정과 서버 호출 파라미터를 Juso 스펙에 맞게 정리
  - `base-url`: `https://business.juso.go.kr/addrlink/addrLinkApi.do` 기준
  - `api-key`는 Juso `confmKey`로 사용
  - 요청 파라미터: `confmKey`, `keyword`, `currentPage`, `countPerPage`, `resultType=json`
- 특수문자(`=` 포함) 키 사용 시 URI 인코딩 문제 없이 호출되도록 수정
- Juso 응답(`results.juso[]`)을 기존 앱 응답 모델(`roadAddress`, `jibunAddress`, `zipCode`)로 매핑
- Juso 오류 코드/타임아웃/장애 상황을 표준 에러 응답으로 매핑(502/504)
- 관련 단위/통합 테스트 보강 및 `./gradlew test` 통과

---

### [x] T-0116 전역 예외 상세 로깅 및 요청 추적성 강화(서버)
**Goal**
- 500 오류 발생 시 원인 클래스/메시지/스택트레이스를 추적 가능하게 하고, 요청 로그와 예외 로그를 `requestId`로 연계해 운영 디버깅 속도를 높인다.

**DoD**
- `GlobalExceptionHandler`의 `Exception` 처리 구간에서 `requestId`, URI, 예외 타입, 메시지, stack trace를 `ERROR` 레벨로 기록
- 기존 표준 에러 응답 포맷은 유지하고, 필요 시 응답에 추적용 `requestId`를 포함(보안상 내부 상세는 미노출)
- 로컬 프로필(`application-local.yml`)에 한해 JPA/Hibernate SQL 및 바인딩 파라미터 로그 레벨 가이드를 추가(운영 기본값은 INFO 유지)
- 민감정보(비밀번호, 토큰, 결제수단 식별값) 로그 비노출 규칙 점검 및 마스킹 정책 문서화
- 재현 가능한 500 시나리오 1건 이상으로 로그 확인 절차 문서화(어디서 터졌는지 확인 가능해야 함)

---

### [x] T-0117 사용자 휴대폰 본인인증 데이터 모델 추가(PortOne 다날)
**Goal**
- 휴대폰 본인인증 완료 이력을 사용자 계정에 영속화하고, 앱 사용 차단 정책의 기준 데이터로 사용한다.

**DoD**
- DB(Flyway): 사용자 본인인증 관련 컬럼/테이블 추가(예: `phone_e164`, `phone_verified_at`, `phone_verification_provider`, `identity_verification_id`, `ci`, `di`)
- 민감 식별값(`ci`, `di`) 저장 정책 명시(암호화/마스킹/접근권한)
- 재인증/중복인증/실패이력에 대한 상태 모델 정의
- 기존 사용자와의 호환성(미인증 상태 기본값) 보장

---

### [x] T-0118 PortOne(다날) 본인인증 시작/완료 API(서버)
**Goal**
- PortOne V2 본인인증 플로우(`requestIdentityVerification`)와 서버 검증 API를 연동한다.

**DoD**
- 본인인증 시작 endpoint 제공(클라이언트에 `storeId`, `channelKey`, `identityVerificationId` 연동 가능 구조)
- 본인인증 완료 endpoint에서 `identityVerificationId`를 받아 포트원 REST API로 `VERIFIED` 상태 검증
- 성공 시 사용자 인증정보/휴대폰 번호 저장, 동일 건 멱등 처리
- 실패 코드/취소/타임아웃 표준 에러 응답 정의
- 시크릿/채널키 환경변수 관리 + 로그 비노출

---

### [x] T-0119 로그인 후 미인증 사용자 앱 사용 차단 정책(서버)
**Goal**
- 휴대폰 본인인증 미완료 사용자는 앱 핵심 기능 접근을 차단한다.

**DoD**
- 로그인/토큰 갱신 응답에 본인인증 상태(`phoneVerificationRequired`) 포함
- 미인증 사용자의 보호 API 접근 차단(예외: 인증 관련 endpoint, 최소 필수 endpoint)
- 차단 시 에러 코드/메시지 표준화(예: `PHONE_VERIFICATION_REQUIRED`)
- 권한체크(RBAC)와 충돌 없이 동작하도록 통합 테스트 추가

---

### [x] T-0120 내정보(/me) 휴대폰 인증정보 노출(수정 불가)
**Goal**
- 인증 완료된 휴대폰 번호를 조회 가능하게 제공하되, 클라이언트에서 임의 수정은 불가하게 한다.

**DoD**
- `/me` 응답에 `phoneNumber`, `phoneVerifiedAt`, `phoneVerificationProvider` 포함
- 사용자 프로필 수정 API에서 휴대폰 번호 변경 차단
- 마스킹/표시 정책(예: `010-****-1234`) 명시
- OpenAPI 문서 및 권한 정책 업데이트

---

### [x] T-0121 계정 식별자 체계 개편(로그인 ID 분리, email 보존)
**Goal**
- 인증/권한/로그 표준 식별자를 `email`에서 `loginId`로 전환하고, `email` 컬럼은 향후 기능을 위해 보존한다.

**DoD**
- DB(Flyway): `users`에 `login_id` 컬럼 추가 및 유니크 제약/인덱스 반영
- 기존 계정 마이그레이션: 초기 `login_id`는 기존 email 값으로 채움(충돌 정책 명시)
- 인증/조회의 기본 식별키를 `login_id`로 전환(엔티티/리포지토리/서비스)
- `email`은 nullable/선택 필드로 보존하고 로그인 식별에는 사용하지 않도록 정책 문서화

---

### [x] T-0122 로그인/회원가입 API ID 기반 전환 + 에러 규격 정렬
**Goal**
- 로그인/회원가입 요청 필드를 `email`이 아닌 `id(loginId)` 기준으로 통일한다.

**DoD**
- `/auth/register`, `/auth/login` 요청/응답 스키마에서 식별자 필드를 `id`(또는 `loginId`)로 정렬
- 로그인 실패 사유 분리 정책(T-0114) 유지: `ID 없음`, `비밀번호 불일치`를 각각 구분
- 로그인 감사로그(T-0108)의 식별자 저장 정책을 `loginId` 기준으로 정렬
- 기존 클라이언트 호환 여부(일시 호환 또는 즉시 단절)와 배포 순서 문서화
  - 문서: `docs/dev/t-0122-auth-id-rollout.md`

---

### [x] T-0123 권한 부여/검색 API 식별자 노출 정책 정렬(ID/이름 중심)
**Goal**
- OPS_ADMIN/SYS_ADMIN 권한 부여 화면에서 사용자 식별 정보를 `ID/이름` 중심으로 노출한다.

**DoD**
- 권한 부여 대상 조회/검색 API 응답에 `loginId`, `name` 포함(필수)
- 화면 계약에서 `email` 의존 제거(필요 시 보조 필드로만 유지)
- 검색 조건 정책 정리: ID/이름 기반 검색 지원, 권한별 대상 필터 유지
- 권한 변경 감사로그에는 처리자/대상의 내부 PK + 표시용 `loginId`를 함께 추적 가능하게 정리
  - 문서: `docs/dev/t-0123-role-grant-id-name-policy.md`

---

# EPIC 2) 기사 신청 → 승인 → DRIVER 역할 추가

### [x] T-0201 DB: driver_applications
**Fields**
- user_id
- status (PENDING / APPROVED / REJECTED)
- payload(JSON)
- processed_by
- processed_at

---

### [x] T-0202 USER: 기사 신청 생성 / 조회 API
### [x] T-0203 OPS_ADMIN: 기사 신청 승인 / 반려 API
### [x] T-0204 승인 시 DRIVER 역할 추가 + 감사 로그
### [x] T-0205 SYS_ADMIN: OPS_ADMIN 권한 부여 / 회수

### [x] T-0206 OPS_ADMIN 기사신청 승인 UX 개선(신청 목록 기반)
**Goal**
- 관리자가 신청 ID를 수동 입력하지 않고, 대기 신청 목록에서 선택해 승인/반려할 수 있게 한다.

**DoD**
- 서버: 기사 신청 목록 조회 API에 상태/정렬/페이징 기준 명확화
- 승인/반려 처리 응답에 처리자/처리시각 포함
- ID 직접입력 없이도 승인 가능한 UI 연동을 고려한 응답 구조 정리

---

### [x] T-0207 OPS_ADMIN 권한 신청/승인 플로우(OPS_ADMIN_REQUEST)
**Goal**
- 사용자(또는 DRIVER)가 OPS_ADMIN 권한을 신청하고 SYS_ADMIN이 승인/반려하는 절차를 추가한다.

**DoD**
- DB: ops_admin_applications(신청자/상태/처리자/처리시각/사유) 테이블 추가
- USER/DRIVER: OPS_ADMIN 권한 신청 API 제공
- SYS_ADMIN: OPS_ADMIN 권한 신청 목록/승인/반려 API 제공
- 승인 시 user_roles에 OPS_ADMIN 추가, 감사로그 기록

---

### [x] T-0208 SYS_ADMIN 권한 신청/승인 플로우(SYS_ADMIN_REQUEST)
**Goal**
- SYS_ADMIN 권한도 직접 ID 부여가 아닌 신청 기반 승인 절차로 관리한다.

**DoD**
- DB: sys_admin_applications(신청자/상태/처리자/처리시각/사유) 테이블 추가
- OPS_ADMIN(또는 정책상 허용된 역할): SYS_ADMIN 권한 신청 API 제공
- SYS_ADMIN: 신청 목록/승인/반려 API 제공(자기승인 방지 규칙 포함)
- 승인 시 user_roles에 SYS_ADMIN 추가, 처리 이력 감사로그 기록

---

### [x] T-0209 DRIVER 권한 신청/승인 정책 정렬(USER 신청 + OPS_ADMIN/SYS_ADMIN 승인)
**Goal**
- USER 계정이 DRIVER 권한을 신청하고, OPS_ADMIN 또는 SYS_ADMIN이 승인/반려할 수 있도록 정책을 명확히 반영한다.

**DoD**
- DRIVER 신청 승인 권한에 SYS_ADMIN 포함(OPS_ADMIN 외 승인 가능)
- 승인 시 `user_roles`에 DRIVER 추가(기존 USER 유지)
- 승인/반려 처리자 역할(OPS_ADMIN/SYS_ADMIN) 및 처리시각 감사로그 기록
- 모바일 권한 신청/승인 화면 정책 문구 일치

---

### [x] T-0210 OPS_ADMIN 권한 부여 대상 검색/선택 정책(대상: DRIVER)
**Goal**
- OPS_ADMIN이 DRIVER 계정을 검색해 OPS_ADMIN 권한을 부여할 수 있도록 대상 제한 정책을 반영한다.

**DoD**
- OPS_ADMIN 권한 부여 대상 조회 API 제공(검색어/페이징)
- 대상 조건: DRIVER 권한 보유 + OPS_ADMIN 미보유
- 권한 부여/회수 처리 시 감사로그 기록
- 모바일(또는 운영 UI)에서 검색 기반 선택 부여 동선 제공

---

### [x] T-0211 SYS_ADMIN 권한 부여 대상 검색/선택 정책(대상: 비 SYS_ADMIN)
**Goal**
- SYS_ADMIN이 SYS_ADMIN이 아닌 계정을 검색해 SYS_ADMIN 권한을 부여할 수 있도록 한다.

**DoD**
- SYS_ADMIN 권한 부여 대상 조회 API 제공(검색어/페이징)
- 대상 조건: SYS_ADMIN 미보유 계정
- 자기 자신 부여/회수 등 금지 정책 명시 및 검증
- 권한 변경 시 감사로그 기록 + 모바일(또는 운영 UI) 검색 기반 선택 동선 제공

---

# EPIC 3) 쓰레기 수거 요청 / 배정 / 측정

## 상태 정의 (MVP)
- REQUESTED: 사용자 신청 완료
- ASSIGNED: 기사 배정 완료
- MEASURED: 기사 무게 입력 + 사진 업로드 완료
- PAYMENT_PENDING: 결제 시도 중
- PAID: 결제 성공
- COMPLETED: 최종 완료
- PAYMENT_FAILED: 결제 실패(운영자 조치 필요)
- CANCELED: 수거 전 취소(정책: REQUESTED에서만 허용)

---

### [x] T-0301 DB: waste_requests / waste_assignments / waste_status_logs / waste_photos
**Goal**
- “쓰레기 수거” 도메인 중심 테이블 설계

**필수 항목(예시)**
- waste_requests:
  - id, user_id
  - address, contact_phone, note
  - status
  - measured_weight_kg (nullable)
  - measured_at (nullable)
  - measured_by_driver_id (nullable)
  - final_amount (nullable)
  - currency(KRW)
  - created_at, updated_at
- waste_assignments:
  - request_id, driver_id, assigned_at
- waste_status_logs:
  - request_id, from_status, to_status, actor_user_id, created_at
- waste_photos:
  - request_id, url, type(OPTIONAL: TRASH/ SCALE), created_at

**DoD**
- Flyway 마이그레이션 + JPA 엔티티/리포지토리

---

### [x] T-0302 상태 전이 규칙 서버 강제 + 감사로그
**DoD**
- 불가 전이 거절(400 또는 409)
- 전이 시 waste_status_logs 기록(누가/언제/무엇)

---

### [x] T-0303 USER: 쓰레기 수거 신청 생성/내 목록/상세/취소
**Endpoints(예시)**
- POST /waste-requests
- GET /waste-requests (본인)
- GET /waste-requests/{id} (본인)
- POST /waste-requests/{id}/cancel

**DoD**
- 생성: REQUESTED
- 취소: REQUESTED에서만 가능 → CANCELED

---

### [x] T-0304 OPS_ADMIN: 전체 요청 목록/필터/상세
**DoD**
- 상태별 필터(REQUESTED/ASSIGNED/MEASURED/PAYMENT_FAILED 등)
- 기본 페이징/정렬

---

### [x] T-0305 OPS_ADMIN: 기사 배정(ASSIGNED 전이)
**DoD**
- REQUESTED → ASSIGNED 전이
- waste_assignments 생성

---

### [x] T-0306 DRIVER: 내 배정 목록/상세
**DoD**
- 본인 배정만 조회 가능

---

### [x] T-0307 사진 업로드 API(최소) + 사진 저장
**Goal**
- DRIVER가 업로드한 사진을 URL로 저장

**DoD**
- POST /uploads (multipart) → URL 반환
- 이미지 확장자/용량 제한
- 개발용: 로컬 저장 OK (운영은 S3 후순위)
- 업로드된 URL을 waste_photos에 저장 가능

---

### [x] T-0308 DRIVER: 무게 입력 + 사진 업로드 완료 처리(MEASURED 전이)
**Goal**
- 기사님이 현장에서 무게 입력 + 사진 업로드 후 “측정 완료”

**요구**
- measured_weight_kg 입력(소수 가능)
- 사진 1장 이상 필수

**DoD**
- ASSIGNED → MEASURED 전이
- measured_* 필드 저장 + waste_photos 저장

---

### [x] T-0309 요금 계산 서비스(무게 기반)
**Goal**
- 무게(kg) × 단가(원/kg) = 금액

**DoD**
- MVP 단가 정책:
  - 기본 상수(예: 1000원/kg) 또는 설정값(ENV/DB) 중 택1
- final_amount 계산 및 저장
- 계산 로직 단위테스트 1개 이상

---

### [x] T-0310 주문번호(orderNo) 발급/저장 + API 응답 포함
**배경**
- 알림톡/CS/조회에서 사람이 읽을 수 있는 “주문번호”가 필요하다.

**Goal**
- waste_request 생성 시 사용자에게 노출 가능한 주문번호를 발급하고 저장한다.

**정책(제안)**
- 형식 예: `WR-{yyyyMMdd}-{id(6자리 패딩)}` 또는 ULID 기반 등 1개로 확정(문서화)

**DoD**
- DB: `waste_requests`에 `order_no`(UNIQUE) 컬럼 추가 + 기존 데이터 백필(가능한 범위)
- POST `/waste-requests` 응답에 `orderNo` 포함
- GET `/waste-requests`(목록) / `/waste-requests/{id}`(상세) 응답에 `orderNo` 포함
- 용어 통일: 화면/문서에서 “주문번호 = orderNo”
- 최소 1개 테스트: 주문번호 생성 규칙 및 UNIQUE 보장

---

### [x] T-0311 수거 신청 데이터 확장: 배출품목 + 수거비닐 수량
**배경**
- 신청/알림톡/상세 화면에서 “배출품목”과 “수거비닐 n개 받기” 정보가 필요하다.

**Goal**
- USER가 수거 신청 시 배출품목 리스트와 수거비닐 수량을 저장/조회할 수 있게 한다.

**DoD**
- DB: `waste_requests`에 아래 필드 추가(또는 별도 테이블. MVP는 단순안 우선)
  - `disposal_items` (JSON 또는 TEXT; 문자열 배열 형태)
  - `bag_count` (INT)
- POST `/waste-requests` 요청 DTO에 `disposalItems[]`, `bagCount` 추가
  - `bagCount`: 0 이상 정수
  - `disposalItems`: 최소 1개 이상 권장(정책 명시)
- 목록/상세 조회 응답에 동일 필드 포함
- OpenAPI 문서에 필드 설명/예시 추가
- 배포 정책: 기존 앱과의 호환성(옵셔널 처리 또는 앱/서버 동시 배포) 명시

---

### [x] T-0312 상세 조회 응답 확장: 기사 사진/무게/결제(예정)금액을 한 번에 제공
**Goal**
- 신청 상세 화면에서 주문번호, 배출품목, 비닐수량, 기사 사진, 무게, 금액을 전부 확인 가능해야 한다.

**DoD**
- USER: GET `/waste-requests/{id}` 응답에 아래 포함(권한: 본인 요청만)
  - `orderNo`, `status`, `address`, `contactPhone`, `note`, `disposalItems`, `bagCount`
  - `photos[]` (DRIVER 업로드 포함)
  - `measuredWeightKg`, `measuredAt`
  - `finalAmount`, `currency`
  - 상태 타임라인(= `waste_status_logs`) 최소 정보(from/to/at)
- OPS_ADMIN: 요청 상세 응답에도 동일 정보 제공 + 배정 정보(driverId/assignedAt) 포함
- 사진 URL 노출 정책 정리(공개 URL이 아니면 signed URL 발급 정책 문서화; 운영은 후순위)
- 최소 1개 테스트: USER의 타인 요청 상세 접근 403, 본인 요청은 사진/무게/금액 포함

---

### [x] T-0313 서비스 지역 화이트리스트(동 단위) 데이터/조회 API
**Goal**
- 수거 신청 가능 지역을 동 단위 화이트리스트로 관리하고 조회할 수 있게 한다.

**DoD**
- DB(Flyway): 서비스 지역 테이블 추가(최소: 시/구/동, 활성여부)
- OPS_ADMIN/SYS_ADMIN용 서비스 지역 등록/비활성화/조회 API 제공
- USER 앱에서 사용할 서비스 가능 지역 조회 API 제공(검색/페이징)
- 도로명주소 기반 데이터와 동 단위 매칭 정책 문서화
  - 문서: `docs/dev/service-area-matching.md`

---

### [x] T-0314 수거신청 생성 시 서비스 지역 검증(화이트리스트 강제)
**Goal**
- 대표 주소지가 화이트리스트에 포함되지 않으면 수거 신청이 생성되지 않도록 서버에서 강제한다.

**DoD**
- 수거 신청 생성 시 주소의 동 단위 판별 후 화이트리스트 검증
- 미포함 시 표준 에러 코드 반환(예: `SERVICE_AREA_UNAVAILABLE`)
- 허용/비허용 케이스 통합 테스트 추가
- 기존 대표주소지 자동적용 로직(T-0530 계열)과 회귀 충돌 없도록 검증

---

### [x] T-0315 수거신청 연락처 소스 전환(입력값 -> 인증된 휴대폰)
**Goal**
- 수거 신청의 연락처를 폼 입력값이 아닌 본인인증 완료 휴대폰 번호로 고정한다.

**DoD**
- 서버 수거신청 생성 DTO에서 `contactPhone` 제거 또는 무시 처리
- 본인인증 미완료 사용자는 신청 생성 차단
- 신청 데이터의 `contact_phone`은 인증 휴대폰 번호만 저장
- 관련 OpenAPI/에러 응답/테스트 갱신

---

### [x] T-0316 OPS_ADMIN 기사 배정 대상 검색 고도화(기사 이름 검색 + DRIVER 필터 강제)
**Goal**
- OPS_ADMIN이 수거 요청 배정 시 기사 `id` 수동 입력 대신 기사 `이름` 검색으로 배정할 수 있게 한다.

**DoD**
- 배정 대상 조회 API 제공(검색어/페이징): 이름 기준 검색 지원, 필요 시 ID 부분검색 병행
- 대상 조건 강제: `DRIVER` 권한 보유 사용자만 조회/선택 가능
- 배정 처리 endpoint는 선택된 기사 내부 PK로만 배정 수행(임의 ID 입력 우회 방지)
- 권한/대상 조건(비DRIVER 제외, 비활성 계정 제외 정책 등) 통합 테스트 추가

---

# EPIC 4) 결제: 저장카드 등록 + 수거 후 자동 결제 (Toss 기반)

> 주의: 공개 레포에 결제 키/시크릿/카드정보 저장 금지.
> 모든 키는 환경변수로만 관리.

### [x] T-0401 DB: payment_methods (사용자 저장 결제수단)
**Goal**
- USER당 결제수단 1개 이상 저장

**필드(예시)**
- user_id
- provider(TOSS)
- customer_key (우리 시스템에서 유저 식별용 키)
- billing_key_or_token (저장결제 식별자)
- status(ACTIVE/INACTIVE)
- created_at, updated_at

---

### [x] T-0402 DB: payments (요청별 결제 기록, 멱등)
**Goal**
- waste_request_id 기준 중복 결제 방지(멱등)

**DoD**
- request_id UNIQUE(또는 provider_order_id UNIQUE)
- status(SUCCEEDED/FAILED/PENDING)
- amount, provider refs 저장

---

### [x] T-0403 카드 등록 시작/완료(콜백) 플로우(최소)
**Goal**
- 사용자가 “결제 카드 등록”을 1회 수행하면 payment_methods에 저장

**DoD**
- 서버:
  - 카드 등록 시작 endpoint → 등록용 페이지 URL 반환
  - successUrl 콜백 endpoint → payment_methods 저장
  - failUrl endpoint
- 보안:
  - customer_key 생성 규칙 문서화
  - 로그에 키/토큰 노출 금지

---

### [x] T-0404 MEASURED 이후 자동 결제 실행(서버)
**Goal**
- DRIVER가 측정(MEASURED) 완료하면 서버가 자동 결제 시도

**DoD**
- 흐름:
  - MEASURED 완료 → 요금계산(T-0309) → 결제수단 확인
  - 결제 시도 → 성공: PAID → COMPLETED
  - 실패: PAYMENT_FAILED
- 멱등:
  - 같은 요청에 대해 중복 결제 시도 방지

---

### [x] T-0405 결제 실패 대응(최소)
**Goal**
- 결제 실패 시 사용자/운영자가 대응 가능

**DoD**
- USER:
  - 결제수단 상태 조회
  - 결제수단 재등록 가능
- OPS_ADMIN:
  - PAYMENT_FAILED 목록 조회
  - 결제 재시도 endpoint(최소)

---

# EPIC 5) 모바일(Expo) - 단일 앱 역할별 UI

### [x] T-0501 Expo TS 앱 생성 + 네비게이션 구성
### [x] T-0502 API 클라이언트 + 토큰 저장 + 자동 refresh
### [x] T-0503 이메일 로그인 UI + /me 기반 역할 분기

## USER
### [x] T-0504 USER: 수거 신청(생성/목록/상세/취소)
### [x] T-0505 USER: 결제수단 등록(등록 페이지 WebView 오픈)
### [x] T-0506 USER: 결제수단 상태/재등록 + 결제 실패 안내

## DRIVER
### [x] T-0507 DRIVER: 배정 목록/상세
### [x] T-0508 DRIVER: 무게 입력 + 사진 업로드 + 측정 완료 처리

## OPS_ADMIN / SYS_ADMIN
### [x] T-0509 OPS_ADMIN: 기사 신청 승인/반려
### [x] T-0510 OPS_ADMIN: 요청 목록/배정/결제 실패 처리
### [x] T-0511 SYS_ADMIN: OPS_ADMIN 권한 부여/회수(최소)
### [x] T-0512 모바일 UI/UX 개선(오늘수거 벤치마크, 로그인 UX 포함)
**Goal**
- 모바일 전반 화면을 MVP 수준에서 사용성이 높은 UI로 개선
- 디자인 방향은 오늘수거 앱 UX를 참고하되, 현재 기능 흐름(USER/DRIVER/OPS_ADMIN/SYS_ADMIN)은 유지

**DoD**
- 로그인 화면:
  - 키보드 오픈 시 입력창(특히 비밀번호)이 가려지지 않도록 개선(KeyboardAvoiding/스크롤 동작 포함)
  - 로그인 실패 시 email 입력값은 유지되고, 비밀번호만 재입력 가능
- 역할별 홈 화면(최소 USER/DRIVER/OPS_ADMIN/SYS_ADMIN) 공통 UI 톤/간격/버튼 스타일 정리
- 모바일에서 로딩/에러/성공 피드백 가시성 개선(문구/배치/대비)
- 기능 동작 변경 없이 UI/UX만 개선

### [x] T-0513 모바일 회원가입 화면 + 가입 플로우
**Goal**
- 앱에서 이메일 회원가입을 완료하고 바로 로그인 상태로 진입한다.

**DoD**
- 회원가입 화면(이메일/비밀번호/이름) 추가
- 입력 검증/에러 메시지/로딩 처리
- 가입 성공 시 토큰 저장 및 역할 분기 화면 진입
- 로그인 화면에서 회원가입 화면으로 이동 동선 제공

---

### [ ] T-0514 모바일 카카오 로그인 화면/연동
**Goal**
- 앱에서 카카오 로그인 버튼을 통해 서버 OAuth 플로우를 수행한다.

**DoD**
- 로그인 화면에 카카오 로그인 CTA 추가
- WebView 또는 브라우저 딥링크 기반 OAuth 완료 처리
- 로그인 성공 시 토큰 저장 및 역할 분기 화면 진입
- 실패/취소 시 사용자 피드백 제공

---

### [ ] T-0515 모바일 구글 로그인 화면/연동
**Goal**
- 앱에서 구글 로그인 버튼을 통해 서버 OAuth 플로우를 수행한다.

**DoD**
- 로그인 화면에 구글 로그인 CTA 추가
- WebView 또는 브라우저 딥링크 기반 OAuth 완료 처리
- 로그인 성공 시 토큰 저장 및 역할 분기 화면 진입
- 실패/취소 시 사용자 피드백 제공

---

### [x] T-0516 모바일 역할 기반 화면 진입 구조 개편(하단 탭 최소화)
**Goal**
- 단일 권한 사용자(USER only)는 USER 화면만 보이도록 하고, 불필요한 하단 선택 탭을 제거한다.

**DoD**
- USER only 계정 로그인 시 USER 홈만 노출(탭 선택 UI 미노출)
- DRIVER 권한 포함 계정은 DRIVER 배정 화면 진입 동선 명확화
- 멀티 권한 계정(예: DRIVER+OPS_ADMIN)은 정책에 따른 진입/전환 UX 정의 및 반영
- 기존 API 기능 동작은 유지하고 네비게이션 구조만 개선

---

### [x] T-0517 DRIVER 전용 배정건 화면 분리/강화
**Goal**
- DRIVER 권한 사용자가 배정받은 건을 빠르게 확인/처리할 수 있는 전용 화면 흐름을 강화한다.

**DoD**
- 배정 목록/상세/측정 완료 액션을 DRIVER 전용 화면으로 정리
- 사용자(USER) 화면과 정보/액션 혼재되지 않도록 분리
- 배정건 상태별 필터 또는 우선순위 표시(최소 1개) 추가

---

### [x] T-0518 USER 신청 화면 도로명 주소 검색 연동
**Goal**
- 수거 신청 시 수동 입력 대신 도로명 주소 검색으로 주소를 선택/입력할 수 있게 한다.

**DoD**
- USER 신청 화면에 주소 검색 입력/결과 리스트 UI 추가
- 서버 주소검색 endpoint(T-0112)와 연동
- 선택한 주소를 신청 폼에 반영(주소 상세 입력칸 분리 포함)
- 검색 로딩/빈 결과/오류 상태 UX 제공

---

### [x] T-0519 OPS_ADMIN 기사신청 승인 화면 개편(목록 선택형)
**Goal**
- 관리자가 기사 신청을 ID 수동 입력 없이 신청 카드/목록에서 선택해 승인/반려하도록 UI를 개선한다.

**DoD**
- OPS_ADMIN 화면에 PENDING 기사신청 목록 표시
- 신청 상세 정보 확인 후 승인/반려 액션 제공
- 처리 완료 항목은 목록에서 즉시 상태 갱신
- 기존 수동 ID 입력 UI 제거 또는 후순위 숨김

---

### [x] T-0520 권한 신청 화면(OPS_ADMIN/SYS_ADMIN) 및 승인 화면
**Goal**
- 권한 부여 기능을 신청 기반으로 바꾸고, 모바일에서 신청/승인 화면을 제공한다.

**DoD**
- USER/DRIVER: OPS_ADMIN 권한 신청 화면 제공
- 정책 허용 역할: SYS_ADMIN 권한 신청 화면 제공
- SYS_ADMIN: OPS_ADMIN/SYS_ADMIN 신청 목록, 승인/반려 UI 제공
- 상태값(PENDING/APPROVED/REJECTED) 및 처리 이력 표시


---

### [x] T-0521 로그인 이후 공통 하단 탭 내비게이션 구성(홈/신청/이용내역/내정보)
**Goal**
- 로그인 이후 사용자 공통 진입 경험을 일관화하기 위해, 하단 고정 탭(홈/신청/이용내역/내정보) 기반 내비게이션을 구성한다.

**DoD**
- 로그인 성공 후 기본 진입을 하단 탭 내비게이션으로 통일
- 탭 4종(홈/신청/이용내역/내정보) 고정 노출 및 탭 전환 상태 유지
- 역할(USER/DRIVER/OPS_ADMIN/SYS_ADMIN)별 접근 가능한 탭/화면 매핑 정책 명확화
- 기존 핵심 플로우(수거 신청, 배정 확인, 권한 요청 등)가 탭 구조에서 끊기지 않도록 라우팅 정리
- iOS/Android에서 탭 UI(안전영역, 키보드, 스크롤) 동작 확인

---

### [x] T-0522 관리자 권한별 화면 스크롤/상태 점검 및 수정
**Goal**
- OPS_ADMIN/SYS_ADMIN 화면에서 발생하는 스크롤 불가 및 상태 확인 어려움 문제를 권한별로 점검하고 수정한다.

**DoD**
- 관리자 권한별(OPS_ADMIN, SYS_ADMIN, 복수 권한) 주요 화면 스크롤 동작 점검 체크리스트 수립
- 스크롤 불가 재현 케이스 수정(컨테이너 높이, nested scroll, keyboard 충돌 포함)
- 권한별 페이지에서 목록/상세/처리 버튼 상태가 정상 노출되는지 검증
- 로딩/빈 상태/오류 상태 UI를 권한별 화면에 일관 적용
- 최소 1개 이상 권한별 화면에 대한 회귀 테스트 또는 수동 검증 시나리오 문서화

---

### [x] T-0523 USER 탭 정책 정렬(내정보 기반 기사신청, 이용내역 강화, 로그아웃 위치 정리)
**Goal**
- USER 계정의 하단 탭 동선을 단순화하고, 기사 신청/이용내역/로그아웃 위치를 정책에 맞게 정렬한다.

**DoD**
- USER 계정에서 기사 신청 진입은 `내정보` 탭 내부 메뉴로만 제공(다른 탭의 중복 진입 제거)
- `이용내역` 탭에 신청 내역 + 실제 이용(처리) 내역이 함께 보이도록 목록/상태 표시 정리
- `로그아웃` 액션은 `내정보` 탭에서만 노출되도록 정리(홈/신청/이용내역에서는 제거)
- 탭/메뉴 정책을 역할별 UX 문구와 일치시키고, 수동 검증 시나리오 문서화

---

### [x] T-0524 권한 신청/승인 메뉴 내정보 하위 메뉴화(역할별 노출 제어)
**Goal**
- 권한 신청/승인 기능을 독립 탭이 아닌 `내정보` 하위 메뉴로 통합하고, 역할별 노출 정책을 명확히 반영한다.

**DoD**
- 권한 신청/승인 진입점을 `내정보` 하위 메뉴로 이동
- USER: DRIVER 신청 메뉴만 노출, OPS_ADMIN 신청 메뉴 미노출
- DRIVER: OPS_ADMIN 신청 메뉴 미노출
- OPS_ADMIN: OPS_ADMIN 신청 메뉴 미노출(신청자 UI 제거)
- SYS_ADMIN: 권한 승인 메뉴(OPS_ADMIN/SYS_ADMIN 신청 처리) 접근 가능
- 역할별 메뉴 노출 기준을 문서/주석/테스트(또는 수동 검증 시나리오)로 명시

---

### [x] T-0525 OPS_ADMIN 권한 부여 UX 강화(검색 기반 부여, OPS_ADMIN 계정 전용)
**Goal**
- OPS_ADMIN 계정에서 사용자 검색을 통해 OPS_ADMIN 권한을 부여할 수 있도록 운영 UX를 강화한다.

**DoD**
- OPS_ADMIN 권한 부여 화면에 사용자 검색/선택/부여 액션 제공(ID 수동 입력 최소화)
- 대상 사용자 조건(정책)은 서버 권한 규칙(T-0210 계열)과 일치하도록 검증
- DRIVER 계정에서는 OPS_ADMIN 부여/신청 관련 메뉴가 노출되지 않음
- OPS_ADMIN 계정에서는 신청이 아닌 부여(운영) 동선 중심으로 UI 정리
- 부여 성공/실패/권한없음 케이스 피드백 및 수동 검증 시나리오 문서화

---

### [x] T-0526 멀티 권한 최고권한 자동 진입 + USER 탭/내정보 정책 재정렬
**Goal**
- 멀티 권한 계정에서 역할 선택 UI 없이 최고 권한 기준으로 화면을 자동 구성하고,
  USER 동선은 `신청` 단순화 + `내정보` 중심(결제/주소/설정)으로 재정렬한다.

**DoD**
- 멀티 권한 계정 로그인 시 역할 선택 없이 최고 권한 기준으로 진입/메뉴 노출
  - 권한 우선순위 정책 명시: `SYS_ADMIN > OPS_ADMIN > DRIVER > USER`
- `신청` 탭은 수거 신청 생성 기능만 제공(결제수단/주소 등록 UI 제거)
- 결제수단 관리는 `내정보 > 결제수단 관리`로 통합하고 카드 등록/변경 동선 제공
- 로그아웃 위치를 `내정보 > 설정 > 로그아웃`으로 고정(다른 화면의 로그아웃 진입 제거)
- 주소 관리를 `내정보 > 주소관리`로 제공
  - 다중 주소 등록/수정/삭제 가능
  - 대표 주소지 지정/변경 가능
  - 수거 신청 생성 시 대표 주소지를 기본 자동 적용
- 역할별 수동 검증 시나리오 문서화
  - 단일 권한(USER) / 단일 권한(DRIVER) / 멀티 권한(DRIVER+OPS_ADMIN, OPS_ADMIN+SYS_ADMIN) 각각 점검

---

### [x] T-0527 USER 결제수단 관리 확장(카드 직접 등록/계좌이체/카카오페이)
**Goal**
- USER가 `내정보 > 결제수단 관리`에서 결제수단을 이미지 시안과 유사한 동선으로 등록할 수 있게 한다.
- 등록 옵션은 `카드 직접 등록`, `계좌이체(토스)`, `카카오페이` 3가지를 제공한다.

**DoD**
- 결제수단 관리 화면:
  - 등록된 수단이 없을 때 Empty State + `결제수단 등록하기` CTA 노출
  - 등록 CTA 선택 시 결제수단 선택 모달(3옵션) 제공
- 카드 직접 등록:
  - 카드번호/유효기간/MMYY/비밀번호 앞 2자리/생년월일(또는 사업자번호) 입력 폼 제공
  - 입력 검증(형식/길이) + 로딩/실패/성공 피드백 제공
- 계좌이체(토스):
  - 토스 연동 등록 플로우(WebView 또는 외부 브라우저) 진입/복귀 처리
  - 등록 성공 시 결제수단 목록/상태 즉시 갱신
- 카카오페이:
  - 카카오페이 등록 플로우(WebView 또는 외부 브라우저) 진입/복귀 처리
  - 등록 성공 시 결제수단 목록/상태 즉시 갱신
- 정책/연동:
  - 서버 결제수단 타입 모델(`CARD`, `TRANSFER_TOSS`, `KAKAOPAY`)과 모바일 표시 정책 일치
  - 자동결제 가능 수단 정책(예: 카드 우선/기타 수단 수동결제)은 문서 또는 화면 문구로 명시
- 수동 검증 시나리오:
  - 미등록 사용자 진입, 3가지 수단 각각 등록 성공/실패/취소 복귀 케이스 포함

---

### [x] T-0528 모바일 선택/입력창 가시성 UX 개선(입력창 기준 UI 상향)
**Goal**
- 모바일에서 선택/입력 UI가 열릴 때 화면 위에 단순 오버레이로 덮지 않고, 현재 페이지가 입력창 기준으로 상향/스크롤되어 가시성을 확보한다.

**DoD**
- 공통 입력 UX:
  - TextInput/선택형 입력(드롭다운, 모달 선택창 등) 활성화 시 포커스된 필드가 키보드/선택창에 가려지지 않음
  - `KeyboardAvoidingView` 또는 동등한 방식으로 iOS/Android 모두에서 입력창이 항상 보이도록 처리
- 화면 동작 정책:
  - 전체 화면 오버레이 우선 방식 대신, 현재 페이지 레이아웃을 유지하면서 입력 필드 중심으로 화면이 위로 이동
  - 입력 완료/닫기 시 원래 스크롤 위치 복귀 또는 자연스러운 위치 유지
- 적용 범위(최소):
  - 로그인/회원가입
  - USER 수거 신청
  - 주소관리
  - 결제수단 관리(카드 직접 등록 폼, 결제수단 선택 입력 포함)
- 품질 기준:
  - iOS/Android에서 키보드 열림/닫힘, 회전, 안전영역 환경에서 레이아웃 깨짐 없음
  - 수동 검증 시나리오(화면별 입력 포커스/스크롤/닫힘 동작) 문서화

---

### [x] T-0529 모바일 상단 잘림 방지 Safe Area 전면 적용(다기종 대응)
**Goal**
- iPhone/갤럭시 등 다양한 기기에서 상단 UI가 노치/상태바에 가려지거나 잘리는 문제를 제거한다.
- 업계 표준 방식(Safe Area 컨텍스트 + 화면별 inset 적용)으로 화면 구조를 정렬한다.

**DoD**
- 아키텍처/정책:
  - 앱 루트에 `SafeAreaProvider` 적용 여부를 점검하고, 누락 시 추가
  - 화면별 컨테이너에 safe area inset(`top`/`bottom`)을 적용하는 공통 정책 수립
  - 전역 `SafeAreaView` 래핑으로 여백을 낭비하는 방식 대신, 화면 특성별 필요한 edge만 적용
- 내비게이션 연동:
  - React Navigation 기본 헤더/탭과 커스텀 화면 컨테이너 inset이 충돌하지 않도록 정리
  - 헤더 숨김 화면/커스텀 헤더 화면에서 상단 잘림 재현 케이스 제거
- 적용 범위(최소):
  - 로그인/회원가입
  - USER 홈/신청/이용내역/내정보
  - 주소관리/결제수단 관리
  - DRIVER/OPS_ADMIN/SYS_ADMIN 주요 목록/상세 화면
- 품질 기준:
  - iOS/Android 주요 해상도(소형/대형)에서 상단 잘림 0건
  - 세로/가로 전환 및 상태바 표시/숨김 조건에서 레이아웃 유지
  - 수동 검증 시나리오(기기별 스크린샷 포함) 문서화

---

### [x] T-0530 대표 주소지 등록 이후 수거신청 실패 이슈 점검/수정
**Goal**
- 대표 주소지 등록 기능 추가 이후 수거신청이 정상 진행되지 않는 문제를 재현하고, 원인을 찾아 수정한다.

**DoD**
- 재현/원인분석:
  - 대표 주소지 등록(신규/변경) 후 수거신청 생성 플로우를 단계별로 재현
  - 모바일 요청 payload, 서버 검증/에러 응답, 주소 매핑 로직을 함께 점검
  - 실패 원인을 케이스별로 분류(주소값 누락, 포맷 불일치, 기본값 주입 오류 등)
- 수정:
  - 대표 주소지 자동 적용 로직과 수거신청 폼/요청 변환 로직을 정합성 있게 수정
  - 대표 주소지 미존재/부분 입력/삭제 직후 등 경계 케이스에서도 신청 가능 또는 명확한 안내 제공
  - 기존 수동 주소 입력 기반 신청 플로우와 회귀 충돌이 없도록 보장
- 테스트/검증:
  - 최소 1개 이상 테스트 추가(단위 또는 통합, 대표 주소지 적용 후 신청 성공 시나리오 포함)
  - 수동 검증 시나리오 문서화:
    - 대표 주소지 신규 등록 후 신청
    - 대표 주소지 변경 후 신청
    - 대표 주소지 삭제/미설정 상태에서 신청
    - iOS/Android 각각 확인

---

### [x] T-0531 USER 수거 신청 생성 UX: 생성 완료 표시 + 상세 화면으로 자동 이동
**Goal**
- 수거 신청 생성 직후 “생성됨” 피드백을 보여주고, 생성한 요청의 상세 화면으로 이동한다.

**DoD**
- 신청 생성 성공 시 토스트/배너(예: “수거 신청이 완료되었습니다”) 노출(2~3초)
- 응답으로 받은 `requestId`(및 `orderNo`가 있을 경우)를 이용해 즉시 상세 화면으로 navigate
- 중복 생성 방지:
  - 생성 버튼 로딩 상태/비활성화
  - 네트워크 실패/타임아웃 시 에러 메시지 + 재시도 동선 제공
- iOS/Android에서 동작 확인(수동 시나리오 문서화)

---

### [x] T-0532 USER 이용내역(목록) → 상세 진입 구조 변경(하단 상세 제거, 새 화면 오픈)
**Goal**
- 목록에서 항목을 선택하면 하단에 상세가 나타나는 방식 대신, 별도 상세 Screen으로 이동한다.

**DoD**
- 목록 아이템 탭 시 `WasteRequestDetailScreen`으로 push(전체 화면)
- 기존 하단 상세 패널/오버레이 제거(또는 MVP 범위에서 미노출)
- 뒤로가기 시 목록 스크롤/필터 상태 유지
- 상세는 섹션 단위로 명확하게 구분(주문정보/배출정보/수거정보/결제정보)

---

### [x] T-0533 USER 수거요청 상세 화면 정보 확장(주문번호/배출품목/비닐/사진/무게/금액)
**Goal**
- 사용자(및 CS)가 상세 화면에서 필요한 정보를 한 번에 확인할 수 있게 한다.

**DoD**
- 상단에 주문번호(`orderNo`)를 고정 노출(복사 버튼 optional)
- 배출품목(`disposalItems`) 리스트 + 수거비닐 `bagCount` 표시
- 기사 업로드 사진 섹션:
  - 썸네일 그리드 + 탭 시 확대(모달/뷰어)
  - 사진이 없을 때 Empty state
- 무게/금액 섹션:
  - `measuredWeightKg`, `finalAmount`(결제예정금액) 표시
  - 측정 전에는 “미측정” 상태로 표기
- 상태 타임라인(stepper) 표시: REQUESTED → ASSIGNED → MEASURED → PAID/COMPLETED 등
- 서버 응답 스키마(T-0312)와 필드명/표기 용어 일치

---

### [x] T-0534 OPS_ADMIN/DRIVER 상세 화면에서도 동일 정보 확인 가능하도록 정렬(필요시)
**Goal**
- 운영/기사가 상세 화면에서 주문번호/사진/무게/금액을 쉽게 확인한다.

**DoD**
- OPS_ADMIN 상세 화면에 `orderNo`, 배출정보, 사진, 무게, 금액 표시
- DRIVER 상세 화면에도 `orderNo` 표시(현장/CS 대응 목적)
- 권한별 정보 노출 정책이 다르면 명시(예: 사용자 연락처는 기사에게만)

---

### [x] T-0535 신청 탭 서비스지역 가드(비서비스 알림 + 진입 차단)
**Goal**
- 대표 주소지가 비서비스 지역이면 신청 화면 진입 시 즉시 안내하고 신청 기능을 차단한다.

**DoD**
- 신청 탭 진입 시 서비스 가능 여부 조회/검증
- 비서비스 지역일 때 알림 문구 노출: `서비스 지역이 아니예요!`
- 신청 폼 대신 안내 UI 노출(신청 버튼 비활성/미노출)
- 로딩/오류/재시도 상태 UX 포함

---

### [x] T-0536 서비스 가능 지역 안내 화면(시/구/동)
**Goal**
- 사용자가 서비스 가능한 지역을 앱에서 직접 확인할 수 있게 한다.

**DoD**
- `서비스 지역 살펴보기` 진입 화면 추가
- 시/구/동 단위 목록/검색 UI 제공
- 빈 결과/로딩/오류 상태 처리
- 백엔드 화이트리스트 조회 API(T-0313)와 연동

---

### [x] T-0537 로그인 후 휴대폰 본인인증 강제 플로우(PortOne 다날)
**Goal**
- 로그인 이후 미인증 사용자는 본인인증 완료 전까지 앱 사용이 불가능해야 한다.

**DoD**
- 로그인 성공 후 `phoneVerificationRequired=true`면 인증 전용 화면으로 강제 이동
- PortOne 다날 본인인증(WebView/브라우저 redirect 포함) 연동
- 인증 완료 시 서버 검증 endpoint(T-0118) 호출 후 앱 진입 허용
- 인증 실패/취소/재시도 UX 제공

---

### [x] T-0538 내정보 휴대폰 인증정보 조회(수정 불가) + 신청 폼 연락처 제거
**Goal**
- 내정보에서 인증된 휴대폰 정보를 확인할 수 있고, 신청 폼에서는 휴대폰 입력을 제거한다.

**DoD**
- 내정보 화면에 휴대폰 번호/인증일시 표시(수정 UI 미노출)
- 수거 신청 폼의 연락처 입력 필드 제거
- 신청 생성 시 서버에서 인증된 휴대폰 번호 사용 정책과 일치
- 미인증 사용자의 신청 동선 차단 메시지 제공

---

### [x] T-0539 로그인 후 휴대폰 본인인증 화면 한글화 + 상단 잘림(Safe Area) 수정
**Goal**
- 로그인 후 진입하는 휴대폰 본인인증 화면의 사용자 문구를 한국어로 통일하고, 상단 UI 잘림 없이 모든 기기에서 정상 노출되게 한다.

**DoD**
- 휴대폰 본인인증 화면/버튼/안내/에러 문구를 한국어로 통일(영문 하드코딩 제거)
- PortOne 다날 인증 과정에서 노출되는 상태 메시지(로딩/실패/취소/재시도) 한국어 UX 문구 정리
- 인증 화면(및 관련 WebView/리다이렉트 컨테이너)에 `SafeArea`/상단 inset을 적용해 노치/상태바 영역 잘림 제거
- iOS/Android 실기기(노치 기기 포함)에서 상단 잘림 없이 인증 플로우 완료 가능
- 수동 검증 시나리오 문서화:
  - 로그인 직후 미인증 사용자 진입
  - 인증 성공/실패/취소 케이스
  - 상단 영역(상태바/노치) 가림 여부 점검

---

### [x] T-0540 PortOne 본인인증 즉시 실패 원인 분석 + 디버깅 로그 가시화
**Goal**
- 휴대폰 본인인증 시작 직후 `본인인증이 취소되었거나 실패했습니다.`로 종료되는 문제를 재현/원인분석하고, 클라이언트/서버에서 실패 원인을 추적 가능한 로그 체계를 마련한다.

**DoD**
- 재현 시나리오 고정:
  - 로그인 직후 미인증 사용자 기준으로 인증 시작 → 즉시 실패 케이스 재현 절차 문서화
  - iOS/Android 각각 최소 1회 재현 확인
- 모바일(React Native/WebView) 로그 강화:
  - `start` 응답(`storeId`, `channelKey`, `identityVerificationId`) 수신 여부 로그
  - WebView `onMessage` 수신 원문/파싱 결과/에러 코드 로그(민감정보 마스킹)
  - `onNavigationStateChange`의 redirect URL 매칭 결과 로그
- 서버 로그/응답 추적 강화:
  - `/user/phone-verifications/start`, `/complete` 요청/응답에 `requestId` 기반 추적 가능
  - PortOne 검증 실패 시 표준 에러 코드 + 원인 구분(취소/미완료/타임아웃/외부장애) 로그
- 설정값 검증 포인트 추가:
  - 환경변수(`storeId`, `channelKey`, 시크릿) 누락/오설정 시 명확한 서버 로그 및 에러 응답
  - redirect URL 정책(허용 도메인/스킴) 불일치 시 원인 식별 가능하도록 점검
- UX 보완:
  - 사용자 메시지는 일반화(보안상 내부 상세 미노출)하되, 개발자가 확인 가능한 추적 키(`requestId`) 노출 정책 정의
- 검증:
  - 정상 인증 완료 1건 + 실패 1건에서 로그만으로 원인 판별 가능해야 함

---

### [x] T-0541 OPS 이상 서비스 신청지역 관리 UI 추가(내정보 > 설정)
**Goal**
- OPS_ADMIN/SYS_ADMIN이 앱에서 서비스 가능지역을 관리할 수 있도록 `내정보 > 설정 > 서비스 신청지역` 진입 화면을 제공한다.

**DoD**
- `내정보 > 설정`에 `서비스 신청지역` 메뉴 추가(노출 대상: OPS_ADMIN/SYS_ADMIN)
- 메뉴 탭 시 신규 화면으로 이동(스택 push)하고 기존 화면 상태 유지
- 신규 화면에서 서비스 지역 목록 조회/검색/등록/비활성화 동선 제공(T-0313 API 연동)
- USER/DRIVER 단독 권한에서는 메뉴 미노출 또는 접근 차단

---

### [x] T-0542 OPS_ADMIN 배정 UI 기사 이름 검색 기반 배정으로 전환
**Goal**
- OPS_ADMIN 수거 요청 배정 UI를 기사 ID 수동입력 방식에서 `기사 이름 검색/선택` 방식으로 전환한다.

**DoD**
- 배정 화면에 기사 검색 입력 + 결과 목록 + 선택 배정 UX 제공
- 결과 목록은 DRIVER 권한 대상만 노출(T-0316 정책 연동)
- 배정 성공/실패/권한없음/결과없음 상태 UX 제공
- 기존 ID 직접 입력 UI 제거 또는 운영자 전용 숨김 처리

---

### [ ] T-0543 수거 신청 단계 상태 문구 전면 한글화(모바일)
**Goal**
- 신청/이용내역/상세 등 모든 사용자 화면에서 상태값을 코드값이 아닌 한글 문구로 일관 표시한다.

**DoD**
- 상태 매핑 정책 정의: `REQUESTED`, `ASSIGNED`, `MEASURED`, `PAYMENT_PENDING`, `PAID`, `COMPLETED`, `PAYMENT_FAILED`, `CANCELED`
- USER/DRIVER/OPS_ADMIN/SYS_ADMIN 화면 공통으로 한글 표기 적용
- 상세 타임라인/배지/목록 카드의 상태 표시를 동일 매핑으로 통일
- 미정의 상태 fallback 문구 및 로깅 정책 추가

---

### [ ] T-0544 대표 주소지 미설정 시 신청 오류 중복 노출 제거
**Goal**
- 대표 주소지가 없을 때 신청 화면에서 동일 의미 오류가 중복 노출되는 UX 문제를 해결한다.

**DoD**
- 대표 주소지 미설정 케이스의 에러 소스(화면 검증/서비스지역 검증/API 실패)를 단일 메시지로 정리
- 중복 렌더링 조건 제거(동일 에러 2회 이상 노출 방지)
- 재시도/주소관리 이동 CTA는 1개만 노출
- 회귀 점검: 대표주소지 없음/불완전/로드 실패 케이스별 메시지 중복 없는지 확인

---

### [x] T-0545 로그인/회원가입/권한부여 화면 용어 전환(email -> ID/이름)
**Goal**
- 모바일 화면의 사용자 식별 용어를 `email` 중심에서 `ID/이름` 중심으로 전환한다.

**DoD**
- 로그인/회원가입 화면 라벨/placeholder/에러 문구를 `이메일` -> `아이디` 기준으로 변경
- 권한 부여/대상 검색 화면의 표시 컬럼을 `아이디/이름` 중심으로 변경
- `/me` 및 세션 표시 영역에서 식별자 노출 정책을 `loginId` 우선으로 정렬
- 백엔드 계약(T-0121~T-0123)과 필드명/문구 불일치가 없도록 통합 점검

---

# EPIC 6) 테스트 및 문서(최소)

### [x] T-0601 Auth + RBAC 통합 테스트
### [x] T-0602 상태 전이 테스트(REQUESTED→ASSIGNED→MEASURED→PAID/FAILED)
### [x] T-0603 요금 계산 단위 테스트
### [x] T-0604 자동 결제 멱등성 테스트(요청별 1회 결제)
### [x] T-0605 운영 문서 작성(원격/로컬 DB, 업로드, 결제 실패 대응)

---

# EPIC 7) (추후) 쿠폰/할인 기능 (MVP 이후)

> 이 섹션은 "예정"이며, MVP에서는 구현하지 않는다.

### [ ] T-0701 DB: coupons / user_coupons / coupon_redemptions
### [ ] T-0702 USER: 쿠폰 코드 등록 API
### [ ] T-0703 결제 금액 계산에 쿠폰 할인 반영
### [ ] T-0704 OPS_ADMIN: 쿠폰 생성/비활성화/발급 관리

---

# EPIC 8) (추후) 알림/알림톡(카카오 등) 연동 준비

> 목표: 수거 신청/수거 완료 시 사용자에게 주문번호 기반 알림을 보낼 수 있도록 데이터 계약과 발행 지점을 정리한다.

### [ ] T-0801 알림 이벤트 스키마 정의 + 템플릿 변수 문서화
**Goal**
- 알림톡/푸시 연동 시 필요한 데이터 필드를 고정하고, 템플릿 변수를 문서화한다.

**DoD**
- 이벤트 2종 정의:
  - `WASTE_REQUEST_CREATED`(신청 완료): `orderNo`, `disposalItems`, `bagCount`
  - `WASTE_REQUEST_MEASURED`(수거 완료/측정 완료): `orderNo`, `measuredWeightKg`, `finalAmount`(예정), `currency`
- 템플릿 변수명(예: `#{orderNo}`) 및 예시 메시지 문서 작성
- 개인정보/민감정보 노출 가이드 포함(기본: 주소/연락처는 템플릿에 미포함)
- `docs/notifications/alimtalk.md` (또는 동등 경로) 문서 추가

---

### [ ] T-0802 서버: 알림 발행 Hook(이벤트) 추가(실제 발송은 후순위)
**Goal**
- 실제 카카오 연동 전이라도, 신청/측정 시점에 “알림 이벤트”가 생성되도록 한다.

**DoD**
- 신청(REQUESTED 생성) 시점에 이벤트 1건 생성
- 측정 완료(MEASURED 전이 + `final_amount` 계산 완료) 시점에 이벤트 1건 생성
- 이벤트 저장 방식(택1, MVP는 DB 저장 권장)
  - DB 테이블(`notification_events`) 저장 (status=PENDING/SENT/FAILED) 또는
  - 로깅/메시지 큐 publish
- 재시도/중복 방지(멱등키: `event_type + waste_request_id`) 정책 명시
- 최소 1개 테스트: 동일 요청에 대해 중복 이벤트 생성 방지

---
